syntax = "proto3";

package api;

enum Status {
  OK = 0;
  ERROR = 1;
  FILE_NOT_FOUND = 2;
  ALREADY_EXISTS = 3;
  CHUNK_NOT_FOUND = 4;
}

message ChunkServer {
  string id = 1;
  string address = 2;
}